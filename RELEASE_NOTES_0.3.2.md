# XXL-JOB Console 0.3.2

此版本基于商店已上架的 0.3.1，集中改进任务操作、日志查看和界面反馈。

## 更新内容

- 所有列表采用更紧凑的带边框表格、选中行高亮、带文字的操作按钮，默认每页 50 条；任务常用操作直接显示，其余操作归入「更多」。
- 写入结果不确定等错误改为悬浮提示，可展开查看接口详情；优化按钮、输入框和下拉框样式。
- CRON 配置变化时立即刷新最近运行时间，并调整配置器与任务弹窗的叠层提示。
- 「执行一次」从当前任务配置读取参数并带入执行表单。
- 新标签页查看日志时，先在新的插件会话中核验日志及执行器权限，再读取日志；日志继续输出时默认滚动至底部，并允许用户手动查看历史内容。

## 兼容与安装

- 继续支持 XXL-JOB Admin 2.3.x 和 3.4.x，插件 ID 为 `io.dbx.xxljob-console`，发布者为 `ezreal-byte`。
- Release 附件是供 DBX Store 审核的六个平台未签名 `.dbxp` 候选包。商店安装版需等待 DBX Store 审核、签名并合并升级 PR。
- 新标签页动态显示 `#ID 日志查看` 需要 DBX 宿主中的对应工作台标题更新；未更新的宿主仍显示通用日志标题。

感谢 [caichangqing1120](https://github.com/caichangqing1120/dbx-plugin-xxl-job) 的原始 XXL-JOB 插件。
